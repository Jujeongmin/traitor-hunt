{
  "name": "traitor-hunt-server",
  "private": true,
  "description": "Traitor Hunt Verse8 game server",
  "scripts": {
    "build": "gameserver-node build",
    "test": "gameserver-node test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@agent8/gameserver-node": "^2.0.1",
    "typescript": "~5.7.3"
  }
}
